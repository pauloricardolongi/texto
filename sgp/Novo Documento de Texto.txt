 mandar pela primeira vez:
git config --list
git init
git remote add origin (endereço).git
git pull origin master
git status
git add .
git status
git commit -m "continuação do commit"
git log --oneline
git push -u origin master

